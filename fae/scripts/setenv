#!/bin/bash

export DJANGO_SETTINGS_MODULE=project.settings

if [ `pwd | grep ^/home/nhoyt/src` ]; then
    export PYTHONPATH=/home/nhoyt/src
elif [ `pwd | grep ^/usr/local/src` ]; then
    export PYTHONPATH=/usr/local/src
fi

if [ -n $1 ]; then
    eval /usr/local/bin/python2.5 $1
fi
